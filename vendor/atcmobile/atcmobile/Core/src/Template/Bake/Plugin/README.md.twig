# {{ plugin }} plugin for Atcmobapp

## Installation

You can install this plugin into your Atcmobapp application using [composer](https://getcomposer.org).

The recommended way to install composer packages is:

```
composer require {{ package }}
```
